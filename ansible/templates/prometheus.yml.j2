global:
  scrape_interval: 15s
scrape_configs:
  - job_name: 'node-exporter'
    static_configs:
      - targets:
{% for host in groups['swarm'] %}
          - '{{ hostvars[host].ansible_host }}:9100'
{% endfor %}
